{
  "name": "hardhat-project",
  "scripts": {
    "start": "hardhat",
    "test": "hardhat test",
    "build": "npx hardhat flatten ./contracts/Marketplace.sol > ./build/Marketplace.sol && npx hardhat export-abi"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0",
    "hardhat-abi-exporter": "^2.10.1",
    "hardhat-gas-reporter": "^1.0.9"
  }
}
